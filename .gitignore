# Ignore all files inside the dataset/ folder
datasets/*
# But do not ignore the dataset/ folder itself
!datasets/

# Ignore all files inside the results/ folder
results/*
# But do not ignore the results/ folder itself
!results/

